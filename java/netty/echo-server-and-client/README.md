# Overview

This is the introductory example from Netty in Action: a netty-based echo server and client.

# Build

```bash
./gradlew build
```

# Run

Start the server:

```bash
./bin/run-server
```

Start the client:

```bash
./bin/run-client
```