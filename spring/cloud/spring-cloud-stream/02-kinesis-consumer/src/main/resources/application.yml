
spring.cloud.stream:
  bindings:
    channel-from-kinesis:
      #destination: bamboo-fds-internal-events
      destination: biz-pri-v2-data

cloud:
  aws:
    region:
      static: us-west-2

logging:
  level:
    com:
      amazonaws: INFO
    org:
      apache:
        http: INFO